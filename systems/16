# Find the service which makes it possible to run specific tasks following a regular
# schedule
To run scheduled tasks you can use cron command (crontab -e to open crontab configuration file. You can add scheduled tasks here one per line)