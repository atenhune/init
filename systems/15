# Find the command that kills the background command’s process
sudo kill (PID) or sudo pkill -9 (PID)

# to find out the PID relating to previous question
# ps -aux | grep "\-f /var/log/syslog" | grep -m1 -v "grep" | awk '{print $2}'