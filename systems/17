# Find the command that allows you to connect via ssh on the VM. (In parallel with
# the graphic session)
1. install ssh 
2. Go to VMs network settings and set up port fowarding or use bridged adapter
3. use command to connect to VM      ssh [user]@[ip] -p [port]
